<!-- I used the same partial for sales and purchase history. It differentiates between the two using the params[:action argument]. A better way of doing it would have been to include local assign variables in the render functionality below -->

<%= render "history" %>