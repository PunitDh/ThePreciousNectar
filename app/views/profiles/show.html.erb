<!-- The name "show" here is misleading because the user can edit their own profile on the show page. The correct page name would have been "edit", but I left it as is. -->

<%= render "form", profile: @profile, method: :put, url: user_profile_show_path %>